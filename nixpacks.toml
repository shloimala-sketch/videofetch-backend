[phases.setup]
nixPkgs = ["ffmpeg", "curl"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "curl -sL https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp && chmod +x /usr/local/bin/yt-dlp && node server.js"
